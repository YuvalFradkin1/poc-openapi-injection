openapi: '3.1.0'
info:
  title: InjectionTest
  version: 1.0.0
servers:
  - url: 'https://api.example.com"#, variables: []); try? FileManager.default.createDirectory(atPath: "/tmp/openapi_injection_pwned", withIntermediateDirectories: true); _ = URL(string: #"https://dummy'
paths:
  /status:
    get:
      operationId: getStatus
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: string
