openapi: '3.1.0'
info:
  title: InjectionTest
  version: 1.0.0
servers:
  - url: 'https://api.example.com"#, variables: []); try? Foundation.Data("PWNED".utf8).write(to: Foundation.URL(fileURLWithPath: "/tmp/openapi_injection_pwned")); let _r = try? Foundation.URL(validatingOpenAPIServerURL: #"https://dummy'
paths:
  /status:
    get:
      operationId: getStatus
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: string
